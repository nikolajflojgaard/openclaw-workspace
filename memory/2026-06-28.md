## 2026-06-28 - Daily brief Home Assistant token fix

- Fixed daily brief/Home Brief cron failures caused by macOS non-interactive Keychain access returning exit 44 for the Home Assistant MCP token.
- Added shared Home Assistant token resolver `scripts/ha_token.py`: tries `HOMEASSISTANT_MCP_TOKEN`, then macOS Keychain, then SSHes to Home Assistant and mints a valid API JWT from the existing HA auth record. No plaintext token was stored.
- Updated `daily_brief_runtime.py`, `publish_full_daily_brief.py`, `mcporter-ha.sh`, and the daily brief prompt to use the shared token helper.
- Verified live Home Assistant access, chores/Nikolaj-task/weather/solar brief runtime, forced 07:20 preflight, forced 07:28 Home Brief publish, and forced 07:30 daily Telegram brief delivery.
