# 2026-05-17

- Added two new stock-playbook reviews and pushed them to GitHub:
  - Palantir (`PLTR`) -> 4/5 gates passed, final verdict **WATCH** because the business passes but valuation is wild.
  - Tesla (`TSLA`) -> 2/5 gates passed, final verdict **FAIL** because Rule of 40, moat, and valuation all fail under the framework.
- Added two more stock-playbook reviews and pushed them to GitHub:
  - Novo Nordisk (`NVO`) -> 3/5 gates passed, final verdict **WATCH** because the moat is real but growth/FCF profile plus leadership transition and valuation are not clean enough.
  - NVIDIA (`NVDA`) -> 4/5 gates passed, final verdict **WATCH** because the business is elite but valuation still fails the framework.
- Added stock-type labels to `stock-playbook/RANKINGS.md` and updated `update_rankings.py` so future ranking rebuilds include type classifications.
- Added a monthly DCA dry-powder rule to the stock playbook: if none of the current holdings are 10% below their 52-week high, deploy only 50% of that month’s new capital and keep 50% as dry powder; if an intact-thesis holding is 10% below, deploy double into that opportunity.
- Wrote and published a new website article for `nikolajflojgaard.me`: `The 5 Gates I Use to Stop Myself Buying Good Stories and Bad Stocks`.
- Expanded that article with the monthly DCA dry-powder rule and the reasoning behind it.
- Wrote and published a follow-up website article for `nikolajflojgaard.me`: `Why Great Companies Still Fail Gate 5`.
- Generated and added custom cover images for both new investing articles on `nikolajflojgaard.me` so they render like real posts instead of text-only entries.
- Confirmed the cleaned-up canonical Google Drive work-doc structure under `DATA - NET`: `General designs`, `KISS General designs`, `Final design`, and `CandP designs`; the older Infosys-specific folder split should be treated as obsolete.
- Created and pushed a new private repo `nikolajflojgaard/work-architecture-playbook` to document the architecture/API workflow, templates, decisions, and the OpenAPI YAML -> PDF generation path.
- Created and packaged a new workspace skill `openapi-spec-workflow` for drafting Swagger/OpenAPI YAML and rendering a readable API specification PDF; branding/logos/fonts will be added later when Nikolaj provides a concrete example.
- Created a new workspace skill `stock-playbook-review` so the stock review system, templates, repo rules, and DCA/dry-powder logic are captured as a reusable skill instead of only living in the repo docs.
- Published the `stock-playbook-review` skill publicly at `nikolajflojgaard/stock-playbook-review-skill`, including the packaged `.skill` artifact for others to use.
- Extended the private repo `nikolajflojgaard/work-architecture-playbook` with an ADR template, a durable OpenAPI artifact folder convention, and a titled API-spec PDF renderer; pushed commit `8a25d1b`.
- Added and pushed a small example API spec artifact in `nikolajflojgaard/work-architecture-playbook` under `artifacts/tdc-net/customer-platform/customer-address-api/`, including OpenAPI YAML, notes, and generated PDF; commit `ea32d7b`.
- Nikolaj approved the current TDC NET API specification structure as the canonical template: standalone TOC page, change-log table, overview, interaction with TDC NET API (security setup, documentation, Chinese walls), specific API section with numbered endpoint entries, and attachments section for plain-text examples.
- Regenerated `stock-playbook/RANKINGS.md` after adding the new reviews.
- Commits pushed:
  - `37c92cd` (`Add PLTR and TSLA stock reviews`)
  - `771b073` (`Add NVO and NVDA stock reviews`)
  - `a8a579e` (`Add stock type tags to rankings`)
  - `a28166a` (`Add stock investing framework article`)
  - `c24545d` (`Add monthly DCA dry-powder rule`)
  - `4c33452` (`Expand investing article with DCA rule`)
  - `c7f2a24` (`Add Gate 5 follow-up article`)
  - `6eb1e35` (`Add cover images for investing articles`)
# 2026-05-17

- Added two new stock-playbook reviews and pushed them to GitHub:
  - Palantir (`PLTR`) -> 4/5 gates passed, final verdict **WATCH** because the business passes but valuation is wild.
  - Tesla (`TSLA`) -> 2/5 gates passed, final verdict **FAIL** because Rule of 40, moat, and valuation all fail under the framework.
- Added two more stock-playbook reviews and pushed them to GitHub:
  - Novo Nordisk (`NVO`) -> 3/5 gates passed, final verdict **WATCH** because the moat is real but growth/FCF profile plus leadership transition and valuation are not clean enough.
  - NVIDIA (`NVDA`) -> 4/5 gates passed, final verdict **WATCH** because the business is elite but valuation still fails the framework.
- Added stock-type labels to `stock-playbook/RANKINGS.md` and updated `update_rankings.py` so future ranking rebuilds include type classifications.
- Added a monthly DCA dry-powder rule to the stock playbook: if none of the current holdings are 10% below their 52-week high, deploy only 50% of that month’s new capital and keep 50% as dry powder; if an intact-thesis holding is 10% below, deploy double into that opportunity.
- Wrote and published a new website article for `nikolajflojgaard.me`: `The 5 Gates I Use to Stop Myself Buying Good Stories and Bad Stocks`.
- Expanded that article with the monthly DCA dry-powder rule and the reasoning behind it.
- Wrote and published a follow-up website article for `nikolajflojgaard.me`: `Why Great Companies Still Fail Gate 5`.
- Generated and added custom cover images for both new investing articles on `nikolajflojgaard.me` so they render like real posts instead of text-only entries.
- Confirmed the cleaned-up canonical Google Drive work-doc structure under `DATA - NET`: `General designs`, `KISS General designs`, `Final design`, and `CandP designs`; the older Infosys-specific folder split should be treated as obsolete.
- Created and pushed a new private repo `nikolajflojgaard/work-architecture-playbook` to document the architecture/API workflow, templates, decisions, and the OpenAPI YAML -> PDF generation path.
- Created and packaged a new workspace skill `openapi-spec-workflow` for drafting Swagger/OpenAPI YAML and rendering a readable API specification PDF; branding/logos/fonts will be added later when Nikolaj provides a concrete example.
- Created a new workspace skill `stock-playbook-review` so the stock review system, templates, repo rules, and DCA/dry-powder logic are captured as a reusable skill instead of only living in the repo docs.
- Published the `stock-playbook-review` skill publicly at `nikolajflojgaard/stock-playbook-review-skill`, including the packaged `.skill` artifact for others to use.
- Extended the private repo `nikolajflojgaard/work-architecture-playbook` with an ADR template, a durable OpenAPI artifact folder convention, and a titled API-spec PDF renderer; pushed commit `8a25d1b`.
- Added and pushed a small example API spec artifact in `nikolajflojgaard/work-architecture-playbook` under `artifacts/tdc-net/customer-platform/customer-address-api/`, including OpenAPI YAML, notes, and generated PDF; commit `ea32d7b`.
- Nikolaj approved the current TDC NET API specification structure as the canonical template: standalone TOC page, change-log table, overview, interaction with TDC NET API (security setup, documentation, Chinese walls), specific API section with numbered endpoint entries, and attachments section for plain-text examples.
- Regenerated `stock-playbook/RANKINGS.md` after adding the new reviews.
- Commits pushed:
  - `37c92cd` (`Add PLTR and TSLA stock reviews`)
  - `771b073` (`Add NVO and NVDA stock reviews`)
  - `a8a579e` (`Add stock type tags to rankings`)
  - `a28166a` (`Add stock investing framework article`)
  - `c24545d` (`Add monthly DCA dry-powder rule`)
  - `4c33452` (`Expand investing article with DCA rule`)
  - `c7f2a24` (`Add Gate 5 follow-up article`)
  - `6eb1e35` (`Add cover images for investing articles`)
- Refined the TDC NET API specification renderer/template in `work-architecture-playbook` using real sample PDFs as the baseline, including extracted TDC NET logo usage and closer document structure/branding.
- The canonical TDC NET API specification template was further locked down as: standalone Table of content page; section 1 Change log as a real Version/Date/Description table; section 2 Overview; section 3 Interaction with TDC NET API with subsections 3.1 Security setup, 3.2 Documentation, 3.3 Chinese walls; section 4 The specific API with 4.1/4.2/4.n endpoint entries containing method + path + plain-language purpose; section 5 Attachments for plain-text JSON/XML examples.
- Nikolaj explicitly wants this TDC NET API specification structure to be the default from now on, and it was baked into the workflow docs, memory, and `openapi-spec-workflow` skill.
- Added work-laptop helper scripts to `nikolajflojgaard/work-architecture-playbook`: `scripts/setup-work-laptop.sh` / `scripts/run-api-spec.sh` for macOS and `scripts/setup-work-laptop.ps1` / `scripts/run-api-spec.ps1` for Windows 11, so the API-spec workflow can run on Nikolaj's work laptop too; pushed commits `fb98e92` and `dbed15d`.
- Added a simple YAML drop-folder workflow to `nikolajflojgaard/work-architecture-playbook`: place YAML files in `spec-drop/inbox/`, run `scripts/process-spec-drop.ps1` on Windows 11, and get generated PDF/HTML outputs in `spec-drop/pdf/` and `spec-drop/html/`; pushed commit `5a1a1fa`.
# 2026-05-17

- Added two new stock-playbook reviews and pushed them to GitHub:
  - Palantir (`PLTR`) -> 4/5 gates passed, final verdict **WATCH** because the business passes but valuation is wild.
  - Tesla (`TSLA`) -> 2/5 gates passed, final verdict **FAIL** because Rule of 40, moat, and valuation all fail under the framework.
- Added two more stock-playbook reviews and pushed them to GitHub:
  - Novo Nordisk (`NVO`) -> 3/5 gates passed, final verdict **WATCH** because the moat is real but growth/FCF profile plus leadership transition and valuation are not clean enough.
  - NVIDIA (`NVDA`) -> 4/5 gates passed, final verdict **WATCH** because the business is elite but valuation still fails the framework.
- Added stock-type labels to `stock-playbook/RANKINGS.md` and updated `update_rankings.py` so future ranking rebuilds include type classifications.
- Added a monthly DCA dry-powder rule to the stock playbook: if none of the current holdings are 10% below their 52-week high, deploy only 50% of that month’s new capital and keep 50% as dry powder; if an intact-thesis holding is 10% below, deploy double into that opportunity.
- Wrote and published a new website article for `nikolajflojgaard.me`: `The 5 Gates I Use to Stop Myself Buying Good Stories and Bad Stocks`.
- Expanded that article with the monthly DCA dry-powder rule and the reasoning behind it.
- Wrote and published a follow-up website article for `nikolajflojgaard.me`: `Why Great Companies Still Fail Gate 5`.
- Generated and added custom cover images for both new investing articles on `nikolajflojgaard.me` so they render like real posts instead of text-only entries.
- Confirmed the cleaned-up canonical Google Drive work-doc structure under `DATA - NET`: `General designs`, `KISS General designs`, `Final design`, and `CandP designs`; the older Infosys-specific folder split should be treated as obsolete.
- Created and pushed a new private repo `nikolajflojgaard/work-architecture-playbook` to document the architecture/API workflow, templates, decisions, and the OpenAPI YAML -> PDF generation path.
- Created and packaged a new workspace skill `openapi-spec-workflow` for drafting Swagger/OpenAPI YAML and rendering a readable API specification PDF; branding/logos/fonts will be added later when Nikolaj provides a concrete example.
- Created a new workspace skill `stock-playbook-review` so the stock review system, templates, repo rules, and DCA/dry-powder logic are captured as a reusable skill instead of only living in the repo docs.
- Published the `stock-playbook-review` skill publicly at `nikolajflojgaard/stock-playbook-review-skill`, including the packaged `.skill` artifact for others to use.
- Extended the private repo `nikolajflojgaard/work-architecture-playbook` with an ADR template, a durable OpenAPI artifact folder convention, and a titled API-spec PDF renderer; pushed commit `8a25d1b`.
- Added and pushed a small example API spec artifact in `nikolajflojgaard/work-architecture-playbook` under `artifacts/tdc-net/customer-platform/customer-address-api/`, including OpenAPI YAML, notes, and generated PDF; commit `ea32d7b`.
- Nikolaj approved the current TDC NET API specification structure as the canonical template: standalone TOC page, change-log table, overview, interaction with TDC NET API (security setup, documentation, Chinese walls), specific API section with numbered endpoint entries, and attachments section for plain-text examples.
- Regenerated `stock-playbook/RANKINGS.md` after adding the new reviews.
- Commits pushed:
  - `37c92cd` (`Add PLTR and TSLA stock reviews`)
  - `771b073` (`Add NVO and NVDA stock reviews`)
  - `a8a579e` (`Add stock type tags to rankings`)
  - `a28166a` (`Add stock investing framework article`)
  - `c24545d` (`Add monthly DCA dry-powder rule`)
  - `4c33452` (`Expand investing article with DCA rule`)
  - `c7f2a24` (`Add Gate 5 follow-up article`)
  - `6eb1e35` (`Add cover images for investing articles`)
# 2026-05-17

- Added two new stock-playbook reviews and pushed them to GitHub:
  - Palantir (`PLTR`) -> 4/5 gates passed, final verdict **WATCH** because the business passes but valuation is wild.
  - Tesla (`TSLA`) -> 2/5 gates passed, final verdict **FAIL** because Rule of 40, moat, and valuation all fail under the framework.
- Added two more stock-playbook reviews and pushed them to GitHub:
  - Novo Nordisk (`NVO`) -> 3/5 gates passed, final verdict **WATCH** because the moat is real but growth/FCF profile plus leadership transition and valuation are not clean enough.
  - NVIDIA (`NVDA`) -> 4/5 gates passed, final verdict **WATCH** because the business is elite but valuation still fails the framework.
- Added stock-type labels to `stock-playbook/RANKINGS.md` and updated `update_rankings.py` so future ranking rebuilds include type classifications.
- Added a monthly DCA dry-powder rule to the stock playbook: if none of the current holdings are 10% below their 52-week high, deploy only 50% of that month’s new capital and keep 50% as dry powder; if an intact-thesis holding is 10% below, deploy double into that opportunity.
- Wrote and published a new website article for `nikolajflojgaard.me`: `The 5 Gates I Use to Stop Myself Buying Good Stories and Bad Stocks`.
- Expanded that article with the monthly DCA dry-powder rule and the reasoning behind it.
- Wrote and published a follow-up website article for `nikolajflojgaard.me`: `Why Great Companies Still Fail Gate 5`.
- Generated and added custom cover images for both new investing articles on `nikolajflojgaard.me` so they render like real posts instead of text-only entries.
- Confirmed the cleaned-up canonical Google Drive work-doc structure under `DATA - NET`: `General designs`, `KISS General designs`, `Final design`, and `CandP designs`; the older Infosys-specific folder split should be treated as obsolete.
- Created and pushed a new private repo `nikolajflojgaard/work-architecture-playbook` to document the architecture/API workflow, templates, decisions, and the OpenAPI YAML -> PDF generation path.
- Created and packaged a new workspace skill `openapi-spec-workflow` for drafting Swagger/OpenAPI YAML and rendering a readable API specification PDF; branding/logos/fonts will be added later when Nikolaj provides a concrete example.
- Created a new workspace skill `stock-playbook-review` so the stock review system, templates, repo rules, and DCA/dry-powder logic are captured as a reusable skill instead of only living in the repo docs.
- Published the `stock-playbook-review` skill publicly at `nikolajflojgaard/stock-playbook-review-skill`, including the packaged `.skill` artifact for others to use.
- Extended the private repo `nikolajflojgaard/work-architecture-playbook` with an ADR template, a durable OpenAPI artifact folder convention, and a titled API-spec PDF renderer; pushed commit `8a25d1b`.
- Added and pushed a small example API spec artifact in `nikolajflojgaard/work-architecture-playbook` under `artifacts/tdc-net/customer-platform/customer-address-api/`, including OpenAPI YAML, notes, and generated PDF; commit `ea32d7b`.
- Nikolaj approved the current TDC NET API specification structure as the canonical template: standalone TOC page, change-log table, overview, interaction with TDC NET API (security setup, documentation, Chinese walls), specific API section with numbered endpoint entries, and attachments section for plain-text examples.
- Regenerated `stock-playbook/RANKINGS.md` after adding the new reviews.
- Commits pushed:
  - `37c92cd` (`Add PLTR and TSLA stock reviews`)
  - `771b073` (`Add NVO and NVDA stock reviews`)
  - `a8a579e` (`Add stock type tags to rankings`)
  - `a28166a` (`Add stock investing framework article`)
  - `c24545d` (`Add monthly DCA dry-powder rule`)
  - `4c33452` (`Expand investing article with DCA rule`)
  - `c7f2a24` (`Add Gate 5 follow-up article`)
  - `6eb1e35` (`Add cover images for investing articles`)
- Refined the TDC NET API specification renderer/template in `work-architecture-playbook` using real sample PDFs as the baseline, including extracted TDC NET logo usage and closer document structure/branding.
- The canonical TDC NET API specification template was further locked down as: standalone Table of content page; section 1 Change log as a real Version/Date/Description table; section 2 Overview; section 3 Interaction with TDC NET API with subsections 3.1 Security setup, 3.2 Documentation, 3.3 Chinese walls; section 4 The specific API with 4.1/4.2/4.n endpoint entries containing method + path + plain-language purpose; section 5 Attachments for plain-text JSON/XML examples.
- Nikolaj explicitly wants this TDC NET API specification structure to be the default from now on, and it was baked into the workflow docs, memory, and `openapi-spec-workflow` skill.
- Added work-laptop helper scripts to `nikolajflojgaard/work-architecture-playbook`: `scripts/setup-work-laptop.sh` / `scripts/run-api-spec.sh` for macOS and `scripts/setup-work-laptop.ps1` / `scripts/run-api-spec.ps1` for Windows 11, so the API-spec workflow can run on Nikolaj's work laptop too; pushed commits `fb98e92` and `dbed15d`.
- Added a simple YAML drop-folder workflow to `nikolajflojgaard/work-architecture-playbook`: place YAML files in `spec-drop/inbox/`, run `scripts/process-spec-drop.ps1` on Windows 11, and get generated PDF/HTML outputs in `spec-drop/pdf/` and `spec-drop/html/`; pushed commit `5a1a1fa`.
- Created a matching Google Drive folder structure inside `Data - NET` for the API spec workflow, then simplified it to the actually useful shape: `API spec drop/` with subfolders `YAML/` and `PDF/`; the earlier `html/` folder was trashed.
- Added `work-architecture-playbook/scripts/sync_drive_specs.py` to poll the Drive `API spec drop/YAML` folder, render new or changed YAML files into PDFs, and upload them to the Drive `API spec drop/PDF` folder; pushed commit `869b695` and scheduled a recurring check job (`dd0c5476-3899-44ed-98d1-2d41f78a58e3`).
- Expanded the same Drive sweep to also track `Data - NET/General designs` for new or updated source/design files, seeded the current folder state as baseline so old files do not trigger false alerts, and pushed commit `d92d37a`.
- Tightened the Drive sweep so both `General designs` and `API spec drop/YAML` react only to newly seen files, not edits to already-known files; pushed commit `aace763`.
- Decided the next larger step is a real internal product tentatively called `Architecture Flow`: an architecture workflow manager built on open-source components, using a web app plus BPMN workflow engine rather than just more scripts.
- Recommended stack for `Architecture Flow` v1: Next.js, NestJS, PostgreSQL, Flowable OSS, bpmn-js, Authentik, Redis + BullMQ, MinIO, existing OpenAPI PDF renderer, and OpenClaw as the AI orchestration layer.
- Wrote durable repo docs in `nikolajflojgaard/work-architecture-playbook` for this product direction: `docs/architecture-flow-v1.md` and `docs/architecture-flow-product-spec.md`, covering product thesis, system architecture, BPMN workflow shape, data model, pages, acceptance criteria, and implementation order.
- Created a dedicated private GitHub repo `nikolajflojgaard/architecture-flow` with copied product docs plus a strong `README.md`, `TASKS.md`, and `.gitignore`, so progress can be tracked in-repo instead of only in chat; initial commit `63a87a2`.
- Scaffolded the `architecture-flow` repo into a real monorepo with `apps/web` (Next.js shell), `apps/api`, `apps/worker`, `packages/shared`, `packages/db`, root `pnpm`/Turbo workspace config, `.env.example`, `docker-compose.yml`, and starter SQL schema `packages/db/schema/0001_initial.sql`; pushed commit `c589292`.
- Updated `architecture-flow/TASKS.md` so scaffold and initial data-model tasks are marked done, and wrote `docs/development-bootstrap.md` to document the current bootstrap choices and next implementation slice.
- Replaced the API bootstrap with a real NestJS structure in `architecture-flow/apps/api`, added a plain-`pg` `DatabaseService`, and added the first DB-backed read path: `GET /v1/work-items` with optional `status` and `limit` query params; pushed commit `1f2de44`.
- Added DB migration and seed scripts to `architecture-flow/packages/db` (`migrate.mjs`, `seed.mjs`), root commands `pnpm db:migrate` / `pnpm db:seed`, and a first live inbox preview in the Next.js web app that fetches `/v1/work-items`; pushed commit `d5448d9`.
- Immediately cleaned a sloppy repo artifact mistake by removing committed Turbo cache/build files and expanding `.gitignore`; pushed cleanup commit `750db4f`.
- Verification note: `pnpm typecheck` passes across the monorepo, but full local DB/runtime verification was blocked on this machine because Docker is not installed (`docker` command missing).
- Important continuity note: when token limits hit, resume from the `Architecture Flow` repo and its `TASKS.md` / docs first, not from chat replay.
- In `architecture-flow`, installed Docker Compose locally, verified the app against a real local stack, fixed migration path resolution and Nest DI/runtime issues, added a live work-item detail slice, and pushed commit `935131f`.
- In `architecture-flow`, added the first real Drive intake backbone: DB-backed `intake_sources`/`intake_events`, a worker `sync:intake` command using `gog drive`, idempotent upsert into `work_items`, verified real ingestion from `Data - NET/General designs`, verified duplicate prevention on a second sync, and pushed commit `d6176bc`.
- In `architecture-flow`, added an intake dashboard/status API (`/v1/intake-sources`), a manual sync trigger (`POST /v1/intake-sources/sync` plus a web button), added work-item audit-event API (`/v1/work-items/:id/audit-events`) and rendered the audit trail in the detail page, updated `TASKS.md` with completed Drive-intake items plus new discovered tasks, and pushed commit `326c77c`.
