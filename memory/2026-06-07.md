- Started/restarted Nikolaj's Home Assistant AdGuard Home add-on (`a0d7b954_adguard`) after it was installed but not answering DNS. Verified `dig @192.168.0.241 google.com` resolves and `dig @192.168.0.241 doubleclick.net` returns `0.0.0.0`, so blocking works.
- Network discovery: Mac is on `192.168.68.113`, DHCP server/default router is TP-Link Deco `192.168.68.1`, DHCP currently advertises DNS `{192.168.0.1, 192.168.68.1}`. Home Assistant is `192.168.0.241/24` via dynamic DHCP from upstream Sagemcom `192.168.0.1`. Whole-network AdGuard still requires changing Deco DHCP DNS to `192.168.0.241` and ideally reserving/static-setting HA's IP first.
- Set this Mac's Wi-Fi DNS manually to `192.168.0.241` with `networksetup -setdnsservers Wi-Fi 192.168.0.241`; verified default `dig doubleclick.net` returns `0.0.0.0`. This only covers the Mac until Deco DHCP DNS is changed.
- Nikolaj changed Deco DHCP DNS. Verified DHCP now advertises `domain_name_server {192.168.0.241}` only, and normal DNS lookups from the Mac block ad domains (`doubleclick.net` and `googleadservices.com` return `0.0.0.0`). Upstream routers still do not block directly, which is expected; clients must use DHCP-provided DNS.
