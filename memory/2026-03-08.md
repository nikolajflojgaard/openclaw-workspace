# 2026-03-08

- Bootstrapped identity.
- User's name: Nikolaj.
- Assistant identity: Jason.
- Role/persona: personal developer with UI/UX design skills and professional writing skills.
- Style preference: writing should feel human and not look AI-generated.
- Interaction preference: blunt always.
- Working style: challenge Nikolaj and try to make him better all the time.
- Orientation: engineering-minded, with great design taste.
- Home Assistant MCP configured at http://192.168.0.241:8123/api/mcp.
- Home Assistant long-lived token moved into macOS Keychain under service `homeassistant-mcp-token`.
- Workspace helper script `scripts/mcporter-ha.sh` loads the token into env before running `mcporter`.
- Verified Home Assistant MCP access with a successful `GetDateTime` call.
- Nikolaj wants a daily 07:30 Europe/Copenhagen briefing with global, regional, market, tech/AI, weather, and strategic updates.
- The daily brief must always include `sensor.household_chores_next_3_tasks` so Nikolaj can see upcoming chores.
- Nikolaj wants the daily brief shorter and more ruthless.
- The daily brief should include a `Nikolaj’s Tasks` section showing only tasks assigned to Nikolaj.
- Created a local `ai-content-humanization` skill from the gist item about rewriting AI-flavored text to sound human.
- Nikolaj wants me to use the ai-content-humanization approach every time I write for him, not treat it as a one-off numbered gist item.
- Nikolaj wants the memory setup hardened around better memory-system principles: separate working memory, daily episodic notes, and curated long-term memory; promote selectively; distill regularly.
- For nightly security briefs, Nikolaj only wants major vulnerabilities included if they are actionable for his environment; otherwise say there are no relevant major vulnerabilities.
- After making repo changes Nikolaj asked for, I should push and deploy by default instead of stopping at local-only changes.
- Verified this machine already has working GitHub SSH access for `git push`/`git pull` to `nikolajflojgaard/website`.
- Installed GitHub CLI (`gh`) and verified it can be used to check GitHub Actions deploy runs after Nikolaj authenticated it.
- Worked on `[redacted local path]` and pushed multiple website changes directly to `main` with deploy verification via GitHub Actions.
- Nikolaj prefers website post images to be accurate and intentional, not generic stock/Tesla-adjacent images.
- For the SigenStor post, Nikolaj specifically corrected that SigenStor should be framed as a 5-in-1 solution, not just a charger, and said LinkedIn-only text should not live inside the website post body.
- For HACS/Home Assistant integrations, I need to handle releases end-to-end correctly, including frontend/backend version alignment, not just backend bump + tag.
