# 2026-06-15

- Nikolaj asked for EV-Drive FSD monitoring. Added `scripts/evdrive_fsd_monitor.py`, seeded state with the current known FSD listing, and scheduled cron job `evdrive-fsd-monitor` for 10:00 and 14:00 Europe/Copenhagen daily. Criteria: FSD, preferred max 70,000 km, budget focus 270,000 DKK. Higher-mileage FSD cars should still be reported and marked. It should stay silent when there is no new match.
- For the wife/second-car search, Long Range is not required. Do not overweight Long Range if a cheaper FSD RWD/standard-range car appears.
- Created pending Skill Workshop proposal `gog-oauth-recovery-20260615-27837b18c8` for recovering `gog` OAuth failures like `invalid_grant`, verifying Drive access, and rerunning the Drive folder sweep.
- Created and applied Skill Workshop proposal `context-resume-handoff-20260615-b01b935e87` as live skill `context-resume-handoff`, for saving a structured resume and spawning a fresh session when context usage reaches about 50k tokens or compaction risk is high.
- Created, revised, and applied Skill Workshop proposal `research-subagent-cleanup-20260615-45ad473443` as live skill `research-subagent-cleanup`: use subagents at Jason's discretion when they genuinely help, not automatically for every research task; clean them up with `close_agent` or OpenClaw `cleanup: delete`.
- Published public-safe GitHub repos for the three reusable skills:
  - `gog-oauth-recovery`: https://github.com/nikolajflojgaard/skill-gog-oauth-recovery
  - `context-resume-handoff`: https://github.com/nikolajflojgaard/skill-context-resume-handoff
  - `research-subagent-cleanup`: https://github.com/nikolajflojgaard/skill-research-subagent-cleanup
  The public `gog-oauth-recovery` repo was sanitized to remove Nikolaj's email, local paths, and Drive folder IDs before publishing.
- Recovered `gog` Google Drive OAuth after `invalid_grant` by running `gog auth add nikolaj.floejgaard@gmail.com --services docs,drive,sheets --timeout 10m --force-consent`. Verified Drive access and reran `work-architecture-playbook/scripts/sync_drive_specs.py`; result was `no_changes`.
- Created pending update proposal `gog-oauth-recovery-20260615-482a82b645` to clarify that `gog-oauth-recovery` can initiate/hold/verify/rerun OAuth recovery, but Nikolaj may still need to complete Google browser login/passkey/2FA/CAPTCHA himself.
- Tried to recover `gog` Drive auth after `invalid_grant`. Two `gog auth add nikolaj.floejgaard@gmail.com --services docs,drive,sheets` attempts opened Google OAuth but timed out before callback. Final read-only Drive check still failed with `invalid_grant`; Google access is not restored yet.
- Applied/activated Skill Workshop proposal `gog-oauth-recovery-20260615-27837b18c8` as live skill `gog-oauth-recovery`: when `gog`/Google Drive OAuth hits `invalid_grant`, expired, or revoked token errors, use the skill to check auth, re-login where possible, verify Drive access, and retry the failed Drive sweep once. If Google requires browser login, passkey, 2FA, or CAPTCHA, ask Nikolaj to complete that step.
- Published Nikolajflojgaard.me release `a081d9a` (`Publish agent ops article`): new article `Agent ops beats agent demos`, new SVG hero `personal-agent-ops-kit.svg`, and new Projects entry `Personal Agent Ops Kit` linking the three public skill repos. GitHub `Deploy to Simply (FTPS)` run `27545984425` completed successfully. Local `npm run build:check` hit the known/local Pagefind Darwin ARM binary issue after Astro build, but `npx astro check && npx astro build` passed.
- Nikolaj explicitly reinforced the operating rule behind `context-resume-handoff` and `research-subagent-cleanup`: for long tasks, avoid losing task context and wasting tokens by writing compact handoff summaries before context-window risk, starting a clean/new session when useful, and using subagents only when they genuinely reduce load or improve parallel investigation. Always collect results and clean up subagent sessions afterward.
- Updated and redeployed the `Agent ops beats agent demos` article in commit `882387f` to explicitly cover context-window token burn, compact handoff summaries, clean continuation sessions, and selective subagents for keeping the main task context lighter. GitHub `Deploy to Simply (FTPS)` run `27546642454` completed successfully.
- Improved and pushed public README documentation for the three reusable skill repos:
  - `skill-research-subagent-cleanup` commit `517e399`: expanded purpose, use cases, installation, runtime notes, safety, and cleanup guidance.
  - `skill-context-resume-handoff` commit `d70f471`: expanded context-window problem, handoff contents, install, workflow, example handoff, runtime notes, and safety.
  - `skill-gog-oauth-recovery` commit `e8d8994`: expanded OAuth failure scope, install/config, recovery workflow, browser approval boundary, verification, rerun, example reports, and safety.
