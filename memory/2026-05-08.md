- For Nikolaj's job architecture context, treat the current platform setup as fixed baseline rather than something to redesign: Azure APIM is the API platform for both internal and external APIs, protected with TDC NET Ping token and OAuth 2; Kafka and Artemis run on-prem; future solution evaluations should assume this as the standing platform reality.
- For Google Drive work context, the main working area is inside the folder `DATA - NET`; future architecture/job documents in Drive should assume that is the primary place Nikolaj wants work organized.
- For Drive-based job architecture workflow inside `DATA - NET`, Infosys source design docs will go in `Infosys designs`, KISSified rewrites will go in `KISS Infosys designs`, and KISS output filenames should always be prefixed with `KISS-` (example: `BPI-FAS-Myview-integration` -> `KISS-BPI-FAS-Myview-integration`).
- In Google Drive under `DATA - NET`, `CandP designs` is another working folder that will contain design requirements and should be checked for future architecture/doc work.
- Drive architecture workflow is being simplified: `General designs` will hold requirement/source docs, `KISS General designs` will hold simplified KISS rewrites, and `Final design` will hold recommended solution docs.
