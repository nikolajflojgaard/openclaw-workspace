- Fixed Nikolaj’s scheduled daily brief pipeline after it failed to read real Home Assistant chores/solar data. Root cause: old reminder-style cron + weak MCP path; replaced with direct Home Assistant REST reads and a proper isolated cron delivery flow.
- Added `scripts/daily_brief_runtime.py` to fetch live HA data (household chores, Nikolaj chores, weather, solar power/home load, solar today, solar yesterday via history) and feed the daily brief.
- Added shared "Top 3 Things Today" decision engine work in `family-ops`: spec in `docs/top-3-engine.md`, engine implementation in `packages/engine/src/top-three/build.js`, and brief adapter in `packages/engine/src/top-three/brief.js`. Current stance: Family Ops app itself is not that useful yet; better treated as an engine/UI lab rather than a standalone product focus.
- Built out Home Brief action-layer product direction and first implementation. Added spec in `Documents/homeassistant-home-brief/docs/ACTION-LAYER.md` and implemented backend/frontend support for `top_action`, `recommended_actions`, broader action reasoning, and card UI visibility.
- Expanded Home Brief further with person-aware chores and richer action metadata (`why_now`, `confidence`, `time_window`), plus card updates to surface those recommendations. Current product assessment: Home Brief/Home Assistant household intelligence is a much better investment area than Family Ops right now.
