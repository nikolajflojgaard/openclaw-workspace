# 2026-06-29

- Enhanced Google Drive file `MCP idea.md` into a 640-line bid-ready architecture note for a company-wide MCP, Agent Gateway and LLM Gateway platform. It now covers the technician support thin slice, target architecture, security/auth model, MCP build standards, tool contracts, governance, operating model, evaluation plan, roadmap, and presentation storyline. The same Drive file ID/link was preserved.
- Published the architecture idea as a public article on `nikolajflojgaard.me`: **"MCP needs an enterprise control plane"**. Added article `src/content/blog/2026/2026-06-29-mcp-needs-an-enterprise-control-plane.mdx` and hero asset `public/images/mcp-enterprise-control-plane.svg`; pushed website commit `b907511`. Validation: `npx astro check` passed with existing warnings/hints, `npx astro build` passed, full `npm run build` still fails locally at the known Pagefind darwin-arm64 wrapper issue after Astro completes. GitHub deploy workflow `28395073616` completed successfully.
- Added Mermaid rendering for blog posts and a request-flow sequence diagram to both Drive `MCP idea.md` and the public MCP article. Website commits: `f3224c4` loads Mermaid on demand from CDN only when Mermaid code fences exist; `b46053f` adds the sequence diagram. Local Playwright verified the article renders two Mermaid SVGs and zero raw Mermaid code blocks. GitHub deploy workflow `28397176405` passed build but was still waiting in Simply FTPS upload when last checked.
- Added a C4-style network/container overview to both Drive `MCP idea.md` and the public MCP article. The article version stays public-safe and shows public internet, company edge/access zone, internal AI platform, MCP domain zone, enterprise API/system zone, and controlled model egress. The Drive version includes internal labels such as Ping, APIM/service mesh, MyView, Dslman, Assurance, Order, Caspian, ServiceNow, and controlled LLM provider egress. Website commit `6ecfbde`; local Playwright verified the article renders three Mermaid SVG diagrams and zero raw Mermaid code blocks. GitHub deploy workflow `28397706483` passed build but was waiting in Simply FTPS upload when last checked; a one-shot follow-up was scheduled for 21:55 Europe/Copenhagen.
- Fixed the Mermaid article contract by adding a `mermaid` tag to the MCP article and gating the PostDetails Mermaid renderer on `data-has-mermaid="true"` derived from post tags. Website commit `142f583`; validation: `npx biome check`, `npx astro check`, `npx astro build`, and local Playwright all passed. Playwright confirmed the article has the Mermaid marker, visible `mermaid` tag, three rendered SVG diagrams and zero raw Mermaid code blocks. Deploy workflow `28398089903` passed build but was waiting in Simply FTPS upload when last checked; a one-shot follow-up was scheduled for 22:00 Europe/Copenhagen.

## 2026-06-29 - Claude-Inspired OpenClaw Skills

- Reviewed `awesomeclaude.ai/awesome-claude-skills` and selected 10 useful skills for Nikolaj's OpenClaw workspace.
- Activated and validated all 10 skills: `design-audit`, `skill-quality-audit`, `spec-blueprint`, `secure-code-review`, `release-notes`, `playwright-webapp-qa`, `pairwise-test-design`, `office-doc-workflow`, `knowledge-wiki-curator`, and `vendor-evaluation`.
- Created private GitHub repo `nikolajflojgaard/openclaw-claude-skills` for the curated skill set.
- Curated skills repo local path: `/Users/nikolajflojgaard/openclaw-claude-skills`.
- Workspace activation commits were pushed to `nikolajflojgaard/openclaw-workspace`; curated skills commits were pushed to `openclaw-claude-skills`.
- GitHub device auth succeeded only after Nikolaj manually opened `github.com/login/device` in a browser on mobile; normal GitHub app/authenticator flow did not expose the device-code box.

## 2026-06-29 - MCP / Agent Gateway Architecture Work

- Read and enhanced Google Drive file `MCP idea.md` into a bid-ready architecture note for enterprise MCP, Agent Gateway, and LLM Gateway ownership.
- The note now positions the effort as an enterprise AI integration platform, not a chatbot.
- Key concepts added: Agent Gateway responsibilities, LLM Gateway responsibilities, remote HTTP MCP standard, tool registry, policy enforcement, audit/trace, data classification, prompt-injection/tool-safety controls, OAuth2/Ping/APIM auth model, audience-bound tokens, scopes, mTLS/workload identity, high-risk action confirmation, governance, operating model, roadmap, and presentation storyline.
- Internal architecture examples include Assurance MCP, Order MCP, and Caspian MCP, with tools such as `customer_health`, `diagnose_service`, `get_modem_status`, `create_incident`, `explain_network_fault`, and `get_best_in_class`.
- Fixed the original Mermaid issue by avoiding invalid node IDs such as `FIBER/COAX`.
- Added a request-flow sequence diagram to `MCP idea.md` showing how the technician request enters Agent Gateway, uses Policy/Registry, goes through LLM Gateway for governed reasoning and drafting, calls MCP tools, reaches downstream APIs, confirms high-risk writes, and records audit/trace.

## 2026-06-29 - Nikolajflojgaard.me MCP Article

- Converted the MCP architecture note into a public-safe article for `nikolajflojgaard.me`: `MCP needs an enterprise control plane`.
- Public article URL: `https://nikolajflojgaard.me/posts/2026/2026-06-29-mcp-needs-an-enterprise-control-plane`.
- Article deliberately avoids internal system names from the Drive note while keeping the architecture argument credible.
- Added a custom hero/OG image for the MCP enterprise control plane article.
- Added Mermaid support to the Astro site so article `mermaid` code blocks render as SVG diagrams instead of raw code.
- Implementation was slimmed down after a heavy Mermaid bundle made Simply FTPS deploy too slow: Mermaid should load only on pages that actually contain Mermaid diagrams.
- Article now contains both a reference architecture flowchart and a request-flow sequence diagram, clarifying where the LLM Gateway participates in the actual runtime flow.
- Local validation performed during the work: `npx astro check`, `npx astro build`, and Playwright checks verifying Mermaid diagrams render as SVG and raw Mermaid code blocks are not visible.
- Known site quirk: local `npm run build` can still hit an existing Pagefind `darwin-arm64` wrapper issue after Astro completes.
- Known hosting quirk: direct `curl` from this machine can receive Simply `455 Security Incident Detected`, including for the homepage, so use GitHub Actions deploy status and browser checks instead of curl alone.
- After adding the sequence diagram, deploy run `28397176405` had GitHub build passed but Simply FTPS upload was still in progress; a follow-up check was scheduled for 21:45 Europe/Copenhagen.
- Live Mermaid rendering was broken after local validation because production CSP in `public/.htaccess` blocked the jsDelivr Mermaid ESM import. Fixed by allowing `https://cdn.jsdelivr.net` in `script-src`; website commit `880581f` deployed successfully in GitHub Actions run `28399341617`. Live Playwright verification returned HTTP 200 with the `mermaid` tag marker, three rendered Mermaid SVG diagrams, and zero raw Mermaid code blocks.
